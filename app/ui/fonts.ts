import { Bitter, Noto_Sans } from "next/font/google";

export const bitter = Bitter({subsets: ['latin'], weight: ['500', '600']});
export const noto = Noto_Sans({subsets: ['latin'], weight: ['300']});